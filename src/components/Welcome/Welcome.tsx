import { Typography } from "antd";

const Welcome = () => {
  return (
    <main style={{ margin: "auto" }}>
      <Typography.Title level={1}>Welcome to Medicine Shop</Typography.Title>
    </main>
  );
};

export default Welcome;
