import { Typography } from "antd";

const WelcomeToShops = () => {
  return (
    <main style={{ margin: "auto" }}>
      <Typography.Title level={2}>Choose Shop</Typography.Title>
    </main>
  );
};

export default WelcomeToShops;
